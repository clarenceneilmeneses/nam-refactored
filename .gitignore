# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Production data dumps — this repo is PUBLIC. These carry the real users table
# (bcrypt password hashes, session tokens) and the full sales history (client
# TINs, addresses, supplier costs, margins). Schema and RPC logic live in
# supabase/migrations/; the data never belongs in git.
02_data.sql
nam_data_postgres_part*.sql
**/nam_data_postgres_part*.sql

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
